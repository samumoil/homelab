{ config, pkgs, ... }:

{
  imports =
    [ # paths of other modules
    ];

  options = {
    # option declarations
  };

  config = {
#    programs.nix-index.enable = true;
#    programs.command-not-found.enable = false;
  };
}
